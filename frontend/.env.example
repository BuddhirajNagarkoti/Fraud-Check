# Copy this file to .env and fill in your values
# For local development the defaults below work out of the box

# WebSocket URL for the FastAPI backend
# Local dev:
VITE_WS_URL=ws://localhost:8000/stream
# Cloud Run (replace with your actual deployed URL):
# VITE_WS_URL=wss://your-cloud-run-service-url/stream
